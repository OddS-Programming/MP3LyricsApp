# 🎵 MP3 Lyrics Manager Pro

<div align="center">

**Профессиональное приложение для редактирования MP3 метаданных и поиска текстов песен на Genius**

[![Java](https://img.shields.io/badge/Java-17%2B-ED8936?style=for-the-badge&logo=java)](https://www.java.com)
[![JavaFX](https://img.shields.io/badge/JavaFX-21-0096FF?style=for-the-badge&logo=openjfx)](https://openjfx.io/)
[![Maven](https://img.shields.io/badge/Maven-3.8%2B-C71A36?style=for-the-badge&logo=apache-maven)](https://maven.apache.org)
[![SQLite](https://img.shields.io/badge/SQLite-3.43-003B57?style=for-the-badge&logo=sqlite)](https://www.sqlite.org/)
[![Selenium](https://img.shields.io/badge/Selenium-4.39-43B02A?style=for-the-badge&logo=selenium)](https://www.selenium.dev/)
[![Genius](https://img.shields.io/badge/Genius-API-red?style=for-the-badge&logo=genius)](https://genius.com/api-clients)
[![License](https://img.shields.io/badge/License-MIT-green.svg?style=for-the-badge)](LICENSE)

[🚀 Быстрый старт](#-быстрый-старт) • [🖥 Интерфейс](#-интерфейс) • [⚙ Архитектура](#-архитектура) • [🐛 Проблемы](#-решение-проблем) • [📞 Контакты](#-контакты)

</div>

---

## 📋 Описание

**MP3 Lyrics Manager Pro** — это профессиональное десктоп‑приложение на JavaFX, которое объединяет:

- ✏️ **Редактор метаданных MP3** (Title, Artist, Album, Year, Genre и т.д.)
- 🔍 **Поиск полных текстов песен** на Genius через **Genius API + Selenium + Chrome**
- 💾 **Авто‑сохранение изменений** в SQLite и в **ID3‑теги самих MP3‑файлов**
- ⚡ **Кэш текстов** в БД — повторный поиск выполняется за 0.1 сек

В отличие от консольных версий, интерфейс полностью графический: три вкладки (Library / Search / History), таблица с живым редактированием ячеек, статус‑бар и прогресс‑бар.

---

## ✨ Ключевые особенности

```text
✅ Красивый графический интерфейс на JavaFX
✅ Живое редактирование тегов в таблице (inline editing)
✅ Авто-сохранение: SQLite + ID3-теги MP3-файла
✅ Поиск текста через Genius API + Selenium (с реальной страницы)
✅ Кэш текстов в SQLite (повторный поиск ≈ 0.1 сек)
✅ Нормализация текста: красивые переносы, [Куплет]/[Припев], без мусора
✅ Гарантированное закрытие Chrome после каждого поиска
✅ PageLoadStrategy.EAGER + отключение картинок для ускорения
✅ Поддержка Windows / Linux / macOS
```

---

## 🚀 Быстрый старт

### Требования

| Компонент       | Версия         | Статус          |
|-----------------|----------------|-----------------|
| **Java JDK**    | 17+            | ✅ Обязательно   |
| **Maven**       | 3.8+           | ✅ Обязательно   |
| **Google Chrome** | Актуальная   | ✅ Обязательно   |
| **ОС**          | Win/Linux/macOS| ✅ Поддерживается|

### 1️⃣ Клонируй репозиторий

```bash
git clone https://github.com/OddS-Programming/MP3LyricsApp.git
cd MP3LyricsApp
mvn clean install
```

### 2️⃣ Создай Genius API Token

1. Перейди на [Genius API Clients](https://genius.com/api-clients)
2. Создай клиента и скопируй **Client Access Token**

### 3️⃣ Запуск приложения

**Windows PowerShell:**
```powershell
$env:GENIUS_API_TOKEN="твой_токен"
mvn clean javafx:run
```

**macOS / Linux (bash/zsh):**
```bash
export GENIUS_API_TOKEN="твой_токен"
mvn clean javafx:run
```

**IntelliJ IDEA:**
- `Run → Edit Configurations…`
- **Environment variables:** `GENIUS_API_TOKEN=...`
- Нажми Run или Shift+F10

---

## 🖥 Интерфейс

### 🎧 Вкладка 1: Library

```text
┌─────────────────────────── Library ──────────────────────────┐
│ [Add MP3]  [Delete]  [Refresh]          [Ready]            │
│────────────────────────────────────────────────────────────│
│ Title         | Artist       | Album     | Year | Genre    │
│────────────────────────────────────────────────────────────│
│ Blinding Lights| The Weeknd   | After Hours| 2019 | Synthwave│
│ ...                                                        │
└────────────────────────────────────────────────────────────┘
```

**Функции:**
- **Add MP3** — выбери файл → jaudiotagger читает теги → сохраняет в SQLite
- **Delete** — удаляет выбранный трек из библиотеки
- **Refresh** — перечитывает список из БД

**Авто‑сохранение:** Редактируешь ячейку → нажимаешь Enter → мгновенно сохраняется в SQLite и в ID3‑теги MP3

### 🔍 Вкладка 2: Search (Lyrics)

```text
┌──────────── Lyrics Search (Genius) ──────────┐
│ Artist: [          ]  Track: [          ]    │
│          [ Search ]                  [====]  │
│─────────────────────────────────────────────│
│                                             │
│ [Интро]                                    │
│ Slam shit                                  │
│                                             │
│ [Куплет 1]                                 │
│ Веду прицельный огонь                      │
│ Я в маскхалате, я фон...                   │
│                                             │
└─────────────────────────────────────────────┘
```

**Алгоритм поиска:**
1. Проверяет кэш в SQLite (если уже искал этот трек) → мгновенно показывает текст ✨
2. Если в кэше нет:
   - Genius API ищет трек → получает URL
   - Selenium открывает Chrome (headless)
   - Ждёт загрузки `div[data-lyrics-container='true']`
   - Извлекает текст через JavaScript
   - Нормализует (переносы, маркеры `[Куплет]` и т.д.)
   - Показывает в TextArea
   - Сохраняет в БД (кэш)

### 📚 Вкладка 3: History

```text
┌────────────────────── History ──────────────┐
│ [Refresh]  [Clear]                          │
│─────────────────────────────────────────────│
│ The Weeknd – Blinding Lights @ 2025-12-17   │
│ [Куплет 1]                                  │
│ I can't sleep until I feel your touch...    │
│                                             │
│ ...                                         │
└─────────────────────────────────────────────┘
```

- Показывает последние ~50 поисков с датой
- `Refresh` — перечитывает из БД
- `Clear` — очищает всю историю

---

## ⚙ Архитектура

### Стек технологий

| Компонент | Технология | Версия | Назначение |
|-----------|-----------|--------|-----------|
| **UI** | JavaFX | 21 | Красивый графический интерфейс |
| **Язык** | Java | 17+ | Основной язык |
| **Сборка** | Maven | 3.8+ | Управление зависимостями |
| **БД** | SQLite | 3.43+ | Локальное хранилище (songs + search_history) |
| **Теги MP3** | jaudiotagger | 2.0.3 | Чтение/запись ID3‑тегов |
| **HTTP** | OkHttp + Gson | 4.11 / 2.10 | API‑запросы к Genius |
| **Браузер** | Selenium + Chrome | 4.39 | Автоматизация для извлечения текстов |

### Структура файлов

```text
MP3LyricsApp/
├── pom.xml                                     # Maven-конфиг
├── README.md                                   # Этот файл
└── src/main/java/com/musicapp/mp3lyricsapp20/
    ├── MP3LyricsGUI.java                      # JavaFX GUI + 3 вкладки
    ├── DatabaseManager.java                   # SQLite: upsert/delete/cache
    ├── SongRecord.java                        # Модель для TableView
    ├── GeniusApiClient.java                   # Genius API /search
    └── GeniusLyricsFetcherSelenium.java       # Selenium + Chrome + текст
```

### База данных (SQLite)

```sql
-- Таблица MP3‑файлов
CREATE TABLE songs (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    title TEXT NOT NULL,
    artist TEXT NOT NULL,
    album TEXT,
    year TEXT,
    genre TEXT,
    lyrics TEXT,
    file_path TEXT UNIQUE,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- Таблица истории поисков (+ кэш текстов)
CREATE TABLE search_history (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    artist TEXT NOT NULL,
    track TEXT NOT NULL,
    lyrics TEXT,                            -- Кэшированный текст
    search_date TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

---

## 🧩 Компоненты проекта

### `MP3LyricsGUI.java`
Главное JavaFX‑приложение. Создаёт три вкладки:
- `createLibraryTab()` — таблица треков + Add/Delete/Refresh
- `createSearchTab()` — поиск через Genius
- `createHistoryTab()` — история с функцией Clear

Ключевые методы:
- `autoSave(SongRecord)` — сохранение в БД и MP3
- `writeTagsToFile(SongRecord)` — коммит ID3‑тегов в файл
- `searchLyrics(artist, track)` — поиск с кэшем

### `DatabaseManager.java`
Работа с SQLite:
- `upsertSong(...)` — вставка/обновление трека
- `deleteSongByFilePath(...)` — удаление трека
- `getCachedLyrics(artist, track)` — поиск текста в кэше
- `saveSearchHistory(...)` — сохранение поиска
- `clearSearchHistory()` — очистка истории

### `GeniusApiClient.java`
Работа с Genius API:
- `searchTopHit(artist, track)` — запрос к `/search?q=...`
- Возвращает `SearchResult(title, artist, url)`

### `GeniusLyricsFetcherSelenium.java`
Извлечение текста со страницы:
- Настройка Chrome:
  - `PageLoadStrategy.EAGER` — ускорение
  - Отключение картинок (меньше трафика)
  - `detach=false` + `quit()` в `finally` (гарантированное закрытие)
- Поиск контейнеров текста: `div[data-lyrics-container='true']`
- Извлечение: `innerText` → если пусто, `textContent`
- `normalizeLyrics()` — чистка мусора и красивое форматирование

---

## ⚡ Оптимизация и ускорение

### Гарантированное закрытие Chrome

```java
finally {
    if (driver != null) {
        try { driver.close(); } catch (Exception ignored) {}
        try { driver.quit(); } catch (Exception ignored) {}
    }
}
```

Важно: без этого остаются висящие процессы `chromedriver.exe` → замедление системы

### Ускорение загрузки страницы

```java
// 1. Не ждём полной загрузки
options.setPageLoadStrategy(PageLoadStrategy.EAGER);

// 2. Отключаем картинки
Map<String, Object> prefs = new HashMap<>();
prefs.put("profile.managed_default_content_settings.images", 2);
options.setExperimentalOption("prefs", prefs);

// 3. Headless-режим (опционально)
if (headless) options.addArguments("--headless=new");
```

**Результат:** поиск текста за 1.5–2 сек вместо 5–8 сек

### Кэш текстов в SQLite

```sql
SELECT lyrics FROM search_history
WHERE artist = ? AND track = ?
ORDER BY search_date DESC LIMIT 1
```

Если ранее этот трек уже искал → показываем текст мгновенно (0.1 сек)

---

## 🐛 Решение проблем

### ❌ Genius API 401 Unauthorized

**Проблема:** неверный токен

**Решение:**
- Убедись, что используешь **Client Access Token**, не Client ID/Secret
- Проверь, что `GENIUS_API_TOKEN` видна в процессе:
  ```java
  System.out.println(System.getenv("GENIUS_API_TOKEN") != null);
  ```

### ❌ Lyrics not found / blocked

**Проблема:** Genius выдал капчу или заблокировал

**Решение:**
- Временно отключи headless: `new GeniusLyricsFetcherSelenium(false)`
- Увеличь timeout в `WebDriverWait` до 20 сек
- Если всё равно не работает — Genius требует ручного решения капчи

### ❌ Chrome не запускается

**Проблема:** Chrome не найден или заблокирован

**Решение:**
- Убедись, что Chrome установлен
- Проверь, что не заблокирован антивирусом
- WebDriverManager автоматически скачает подходящий chromedriver

---

## 📊 Производительность

| Операция | Время | Примечание |
|----------|-------|-----------|
| Импорт MP3 (читать теги) | ~50 мс | на файл |
| Сохранение в БД | ~10 мс | `upsertSong` |
| Коммит ID3‑тегов в MP3 | ~100 мс | `audioFile.commit()` |
| Поиск текста (первый раз) | 3–5 сек | Genius API + Selenium |
| Поиск текста (из кэша) | ~100 мс | SELECT из БД |
| Закрытие Chrome | ~1 сек | `driver.quit()` |

---

## 🔐 Безопасность и лицензия

### Условия использования

- Используем API **только для личного / образовательного использования**
- Не кэшируем тексты на публичных серверах
- Соблюдаем авторские права (музыка защищена copyright)

### Лицензия

Проект распространяется под **MIT License**. Смотри [LICENSE](LICENSE).

---

## 🔮 Планы на будущее

- [ ] Встраивание текста в ID3‑тег `USYNC Lyrics`
- [ ] Альтернативные источники текстов (Musixmatch, AZLyrics)
- [ ] Экспорт/импорт библиотеки в JSON/CSV
- [ ] Встроенный мини‑плеер (JavaFX MediaPlayer)
- [ ] Dark mode 🌙
- [ ] Синхронизация с облаком (Google Drive, OneDrive)

---

## 🤝 Помощь проекту

Нашёл баг? Хочешь добавить фичу?

1. Открой [Issues](https://github.com/OddS-Programming/MP3LyricsApp/issues)
2. Проверь, не дублируется ли
3. Создай подробное описание

**Процесс контрибьюции:**
```bash
1. Fork репозиторий
2. git checkout -b feature/amazing-feature
3. git commit -m "Add awesome feature"
4. git push origin feature/amazing-feature
5. Открой Pull Request
```

---

## 📞 Контакты

<div align="center">

🤖 **Telegram:** [@TEARS_UNDER_SEOUL_HIGHWAY](https://t.me/TEARS_UNDER_SEOUL_HIGHWAY)

</div>

---

## 🙏 Благодарности

- **Genius.com** — открытый API для поиска текстов
- **Selenium** — браузерная автоматизация
- **jaudiotagger** — работа с MP3‑метаданными
- **JavaFX** — красивый кроссплатформенный UI
- **Вам** — за использование приложения! ❤️

---

<div align="center">

**Сделано с ❤️ для любителей музыки и чистого кода**

⭐ Если проект понравился — поставь звезду! ⭐

</div>
